[alembic]
script_location = migrations
prepend_sys_path = .
version_path_separator = os
path_separator = os

# The actual URL is supplied at runtime by migrations/env.py via
# core.config.get_settings().database_url (RECON_DATABASE_URL env var) so
# it never has to be duplicated — or hardcoded with credentials — here.
sqlalchemy.url = driver://user:pass@localhost/dbname

[post_write_hooks]

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
